import {GET_WEATHER, SET_ERROR} from '../../types';
import {getWeather as myVal} from '../weatherActions';
import WeatherRepository from '../../../repositories/WeatherRepository';

// TODO:
// describe('weather fetch completed action', () => {
//   it('should return action of GET_WEATHER with correct data', (object, method) => {
//   });
// });
